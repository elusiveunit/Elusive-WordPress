<snippet>
	<content><![CDATA[<?${TM_PHP_OPEN_TAG:php} if ( \$${1:var}->have_posts() ) : ?>

	<?${TM_PHP_OPEN_TAG:php} while ( \$${1:var}->have_posts() ) : \$${1:var}->the_post() ?>
		$0
	<?${TM_PHP_OPEN_TAG:php} endwhile; ?>

<?${TM_PHP_OPEN_TAG:php} endif; wp_reset_postdata(); ?>]]></content>
	<tabTrigger>wploopche</tabTrigger>
	<scope>source.php</scope>
	<description>Loop, custom, HTML [Expanded]</description>
</snippet>