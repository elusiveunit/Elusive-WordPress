<snippet>
	<content><![CDATA[<?${TM_PHP_OPEN_TAG:php} if ( \$${1:var}->have_posts() ) : while ( \$${1:var}->have_posts() ) : \$${1:var}->the_post() ?>
	$0
<?${TM_PHP_OPEN_TAG:php} endwhile; endif; wp_reset_postdata(); ?>]]></content>
	<tabTrigger>wploopch</tabTrigger>
	<scope>source.php</scope>
	<description>Loop, custom, HTML</description>
</snippet>